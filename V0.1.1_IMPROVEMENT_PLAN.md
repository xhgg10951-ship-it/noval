# V0.1.1_IMPROVEMENT_PLAN.md

> **Status: FROZEN**
>
> Version: `v0.1.1-final`
>
> Frozen Date: `2026-08-22`
>
> 本文件取代之前的 `V0.1.1_IMPROVEMENT_PLAN.md` 草案。

## 1. 目标

v0.1.1 不重写现有 Java + Spring Boot + MyBatis + MySQL + Python/FastAPI/LangChain + Vue3 架构。

本版本只解决已经由真实运行和全仓库代码审查确认的问题：

1. Planner 新阶段不能正确续写；
2. Writer 实际没有使用 Structured Memory / Current State；
3. 每章过短且没有目标字数；
4. ChapterPlan 对 Writer 的约束太弱；
5. 已确认计划无法安全中途调整；
6. 生成章节无法人工修改、重写、润色；
7. 缺乏数百章级长篇节奏；
8. Summary 形成低价值细节正反馈；
9. Memory 分类、重要度、范围和去重不足；
10. Generation Job 在 Extraction Retry、Continuous、Stage 完成状态上存在可靠性问题；
11. v0.1 使用 Mock 验收替代了真实 LLM 语义验收。

核心目标：

> 让作者能够在长期故事目标约束下持续续写，控制章节长度和章节目标，随时修改 AI 草稿，并让 Memory 真正辅助而不是主导剧情。

---

## 2. 需求冻结规则

从本文件开始，v0.1.1 **需求冻结**。

Coding Agent 只能：

- 实现本文档 MUST；
- 修复阻塞本文档验收的 Bug；
- 做实现 MUST 所需的最小重构；
- 增加测试、日志和可观察性。

不得因为“以后可能需要”“简历更好看”“更专业”新增功能。

新需求一律记录为：

`v0.1.2 / v0.2 candidate`

除非项目所有者显式解冻。

---

## 3. 冻结 MUST Scope

### MUST-01 Planner Continuation Context

Planner 必须真正获得：

- Story Core Idea
- Story Constraints
- Current State
- Relationship State
- Selected Story Memory
- 最近 2–3 章摘要
- Last Chapter Ending
- Continuation Anchor
- Current Chapter Number
- 已完成 Stage 摘要
- Current Arc
- Target Chapter Count

新 Stage 必须从现有剧情继续，而不是重新开书。

### MUST-02 Writer Context Wiring

Writer 必须真正获得：

- Story Constraints
- Long-form Position
- Current Arc
- Stage Direction
- Current ChapterSpec
- Current State
- Relationship State
- Selected Story Memory
- Recent Narrative Context
- Last Chapter Ending

禁止正式实现继续传空的：

`currentState / storyMemories / relationshipState`。

### MUST-03 ChapterSpec + Length

现有 ChapterPlan 扩展为最小 ChapterSpec：

- `goal`
- `expectedProgress`
- `targetCharacters`
- `mustAdvance`
- `mustNotDo`
- `storyBeats`
- `endingIntent`

作者可以设置 Story 默认章节目标字数，Stage/Chapter 可以覆盖。

### MUST-04 Replan Remaining

Stage 开始生成后，作者可以重新规划**未生成部分**。

必须：

- 已生成 Chapter 不变；
- 已发生剧情不回滚；
- 旧剩余 Plan 不物理删除；
- 新 Plan 从当前 Story State 继续；
- Writer 只使用当前 active Plan。

### MUST-05 Chapter Revision

已生成 Chapter 支持：

- Manual Edit
- Regenerate
- AI Polish
- Revision History
- Approve

AI 输出默认为 Draft。

### MUST-06 Revision → Memory Refresh

章节当前正文变化后：

- 原 Extraction 标记 `STALE`
- 重新提取
- 清理/失效该章节旧派生 Memory
- 不得继续保留正文已经删除的事实

### MUST-07 Long-form Pace

Story 支持：

- `targetChapterCount`
- `defaultTargetCharacters`
- `writingStyle`

新增最小：

`Story → Arc → Stage → Chapter`

不增加 Volume。

Planner 必须知道当前处于全书第几章、哪个 Arc、Arc 的目标和范围。

### MUST-08 Memory v2

Memory 至少支持：

类型：
- CURRENT_STATE
- RELATIONSHIP
- PLOT_FACT
- PLOT_THREAD
- FORESHADOWING
- WORLD_RULE
- TRANSIENT_DETAIL

重要度：
- `1–5`

Scope：
- CHAPTER
- STAGE
- ARC
- STORY

同时实现结构化去重 v1。

### MUST-09 Generation Reliability

必须修复：

- Chapter 已保存后 Memory Extraction 失败的 Retry；
- Stage `ACTIVE → COMPLETED`；
- Continuous 同步阻塞；
- Pause；
- Stop；
- Job / Chapter / Plan checkpoint 一致性。

### MUST-10 Real-LLM Semantic Acceptance

Mock 只能证明工程 plumbing。

以下能力必须真实 LLM 验证：

- Planner 续写；
- ChapterSpec 遵循；
- 章节长度；
- Memory 对后续正文的实际影响；
- 低价值细节不劫持剧情；
- 600 章节奏；
- Polish 保持事实。

---

## 4. 明确 Out of Scope

v0.1.1 不实现：

- RAG
- Embedding
- Vector DB
- GraphRAG
- Knowledge Graph
- Temporal Truth Engine
- Canon Governance
- LangGraph
- Spring AI
- Redis
- Kafka / RabbitMQ / RocketMQ
- Spring Cloud
- Kubernetes
- Multi-provider Routing
- Login / Multi-user
- Payment
- 自动发布小说平台
- AI Detection Bypass
- 商业级富文本编辑器
- 600 章全量自动生成测试
- Volume 层
- Fine-tuning
- 自动多 Agent Story Room

---

## 5. 架构冻结

继续遵守：

> **AI proposes. Java decides. MySQL remembers.**

增加：

> **The plan guides. Memory supports.**

> **Generated text is a draft until the author accepts it.**

Java/Spring Boot 负责业务状态、Context Assembly、Plan/Chapter/Revision/Job/Memory 状态和 MySQL。

Python 负责 Planner、Writer、Memory Extractor、Polish、Query。

Python 不直接修改核心业务表。

---

## 6. 已确认根因

### RC-01 Planner 上下文契约存在，但 Java 没接入

`PlanStageRequest` 已有 `currentState / storyMemories / recentContext`，但当前 `StagePlanningService` 实际传空列表和空字符串。

结果：

> Planner 创建新 Stage 时几乎不知道之前剧情。

这是“每个阶段重新开书”的直接根因。

### RC-02 Writer Memory 契约存在，但 Java 没接入

`GenerateChapterRequest` 已有 `currentState / storyMemories / relationshipState`，但当前 `ChapterGenerationService` 实际仍传空列表。

因此：

> v0.1 的 Structured Memory 没有真正参与 Writer 正文生成。

### RC-03 Agent 状态文件错误宣告完成

`.agent/STATE.md` / Experiment 曾声明 Writer Context 已使用 Memory，但当前 main 代码与声明不一致。

从 v0.1.1 开始：

> DTO 存在、设计存在、Mock 测试存在都不能算功能 DONE。

### RC-04 “面包循环”主要是 Summary Feedback Loop

当前 Writer 没有收到 Story Memory，因此不能再把“面包反复出现”主要归因于 Memory。

更符合代码事实的是：

`Chapter N 的偶然细节 → Summary N 强化 → Chapter N+1 只拿上一章 Summary → 再次强化`

同时 Memory 分类污染是另一个独立问题。

### RC-05 ChapterPlan 信息在 Writer 前丢失

数据库有 `goal + expected_progress`，Writer 实际只得到 `goal`。

v0.1.1 要求 ChapterSpec 从：

`Planner → DB → Java → Python Writer`

完整传递。

### RC-06 系统从未定义正确章节长度

Story、ChapterPlan、AI Request、Prompt 都没有目标字数。

所以 500 字不是偶发 Bug，而是缺需求。

### RC-07 没有作者编辑工作流

Chapter API / Service / UI 目前只有生成和读取，没有 Update / Regenerate / Polish / Revision。

### RC-08 现有 Replan 不能直接开放给 ACTIVE Stage

当前 `replacePlans()` 删除整个 Stage 的 Plan；Chapter 的 `plan_id` 删除后会 `SET NULL`。

如果简单开放 ACTIVE Replan，会破坏历史 Plan 关系和 Generation Job 一致性。

因此必须实现 `Replan Remaining`，不能只加前端按钮。

### RC-09 没有长篇位置模型

Planner 不知道这是 20 章还是 600 章，也不知道当前处于全书哪里，所以自然会用最短路径完成剧情。

### RC-10 Memory Extraction Retry 有状态机 Bug

当前：

`Generate → Save Chapter → Extract Memory`

如果 Save 成功、Extract 失败：

- Chapter 已存在；
- Job index 未增加；
- Retry 时生成器会因为该 plan 已有 Chapter 而跳到下一 Plan；
- 失败章节的 Memory 可能永远不补。

必须引入 Chapter 级 `memoryExtractionStatus`。

### RC-11 Continuous Mode 当前同步阻塞 HTTP 请求

当前后端在请求线程里 `while` 连续生成到完成。

因此无法可靠：

- 实时进度
- Pause
- Stop
- 中途 Replan

v0.1.1 使用 Spring 内部 TaskExecutor / Async 即可，不引入 MQ。

### RC-12 Stage 生命周期不完整

GenerationJob 完成后 Stage 仍可能保持 ACTIVE。

v0.1.1 必须在 active plan 全部完成后将 Stage 标记 COMPLETED。

### RC-13 Memory 类型缺安全边界

Python type 是自由字符串；Java 除 CURRENT_STATE / RELATIONSHIP 外全部 catch-all 写入 StoryMemory。

未知类型不得继续自动变成长记忆。

### RC-14 StoryMemory 无去重、重要度和范围

当前为 INSERT-only。长期运行必然积累大量等价事实。

### RC-15 Inventory 一个槽位会覆盖多个物品

不能所有物品都使用同一个 `field=inventory`。

v0.1.1 使用最小多物品表示，例如：

`field=item:<normalized-name>`。

### RC-16 v0.1 语义验收不成立

v0.1 可以定义为：

> **Engineering Pipeline Accepted**

但不能定义为：

> **Real-LLM Product Behavior Accepted**

因为 Mock Writer 是固定输出，且部分 Baseline/Memory 结论来自架构推断而非真实对照运行。

---

## 7. 冻结领域模型

```text
Story
├── Story Constraints
├── Long-form Settings
├── Arc
│   └── Stage
│       ├── Plan Version
│       │   └── ChapterSpec
│       └── Chapter
│           └── ChapterRevision
├── Current State
├── Relationship State
├── Story Memory
└── Memory Candidate
```

不增加 Volume。

---

## 8. Story Long-form Settings

新增：

- `defaultTargetCharacters`
- `targetChapterCount`
- `writingStyle`

默认建议：

`defaultTargetCharacters = 3000`

---

## 9. Arc

Arc 表示数十章级宏观剧情阶段。

最小字段：

- id
- storyId
- title
- goal
- targetStartChapter
- targetEndChapter
- status

采用 Rolling Planning：

`Story Target → Arc 粗规划 → 当前 Stage 规划 → 当前 ChapterSpec 详细规划`

禁止一次详细规划 600 章。

---

## 10. ChapterSpec

最小字段：

- id
- stageId
- planVersion
- chapterOrder
- goal
- expectedProgress
- targetCharacters
- mustAdvance
- mustNotDo
- storyBeats
- endingIntent
- active
- status

列表字段可以先存 JSON/TEXT，不拆大量子表。

---

## 11. Writer Priority

Writer Context 优先级冻结为：

1. Hard Constraints
2. Long-form Position
3. Current Arc
4. Current Stage
5. Current ChapterSpec
6. Current State
7. Selected Relevant Memory
8. Recent Narrative Context

规则：

> ChapterSpec 控制本章要写什么，Memory 只负责补充事实。

与 ChapterSpec 无关的低价值 Memory 可以完全不出现。

---

## 12. Chapter Length

最终 target：

`Chapter override > Stage override > Story default`

Java 保存：

- targetCharacters
- actualCharacterCount

第一版验收允许目标 ±25%。

---

## 13. Chapter Revision

新增 `chapter_revision`：

- id
- chapterId
- versionNumber
- content
- sourceType
- createdAt

sourceType：

- AI_GENERATED
- MANUAL_EDIT
- AI_POLISH
- AI_REWRITE

Chapter 新增：

- currentRevisionId
- status
- memoryExtractionStatus

Chapter status 只使用：

- DRAFT
- APPROVED

---

## 14. AI Polish

新增 `/ai/polish-chapter`。

Polish 默认：

允许：
- 改句式
- 改对话自然度
- 减少重复
- 改场景表现
- 降低机械总结

禁止：
- 改核心事件
- 新增重大设定
- 改人物最终状态
- 改 Ending Intent

不承诺绕过 AI 检测。

---

## 15. Planner Context v2

Planner 至少收到：

- Core Idea
- Constraints
- Target Chapter Count
- Current Chapter Number
- Current Arc
- Completed Stage Summaries
- Current State
- Relationship State
- Selected Memory
- Recent Chapter Summaries
- Continuation Anchor
- New Stage Direction

Continuation Anchor 至少：

- lastChapterNumber
- currentLocation
- activeCharacters
- currentImmediateGoal
- lastChapterSummary
- lastChapterEnding

系统 Prompt 明确：

> You are continuing an existing story.

---

## 16. Recent Narrative Context

不再只传单个上一章 Summary。

第一版：

- previous 2–3 chapter summaries
- last chapter ending excerpt

Summary 重点记录：

- 本章完成事件
- 状态变化
- 未完成动作
- 结尾位置

避免把普通装饰细节长期强化。

---

## 17. Replan Remaining

示例：

```text
Plan V1 = 1–9
已生成 = 1–3
作者把剩余 6 章改为 3 章
```

执行：

- Plan 1–3 保留
- V1 Plan 4–9 → inactive / superseded
- 创建 V2 Plan 4–6
- 新计划从当前真实 Story State 继续

不得删除已生成 Chapter 对应的历史 Plan。

---

## 18. Continuous Generation v2

改为：

```text
POST start
→ 创建 Job
→ 快速返回 Job
→ 后台 worker 执行
→ 前端 GET job 轮询
```

使用 Spring TaskExecutor / @Async / ExecutorService。

不使用 MQ。

Pause：

> 当前单章安全完成后停。

Stop：

> 不再执行剩余 Plan。

不要求强制中断已经发出的 LLM HTTP Request。

---

## 19. Recovery

系统不能只靠 `currentPlanIndex` 判断恢复位置。

必须根据事实：

- active ChapterSpec
- Chapter 是否存在
- Current Revision 是否存在
- memoryExtractionStatus

决定 Next Safe Action。

例如：

```text
Chapter exists + Memory FAILED
→ retry memory extraction

Chapter missing
→ generate chapter
```

---

## 20. Memory v2

类型固定为：

- CURRENT_STATE
- RELATIONSHIP
- PLOT_FACT
- PLOT_THREAD
- FORESHADOWING
- WORLD_RULE
- TRANSIENT_DETAIL

Importance：

- 5 Story-critical
- 4 Arc-important
- 3 Stage-useful
- 2 Supporting
- 1 Transient

Scope：

- CHAPTER
- STAGE
- ARC
- STORY

示例：

```text
天帝身份
importance=5
scope=STORY

普通面包
type=TRANSIENT_DETAIL
importance=1
scope=CHAPTER
```

---

## 21. Memory Selection

Writer 不得传全部 StoryMemory。

Always：
- Current State
- 当前人物 Relationship
- importance=5 active memories

Relevant：
- 当前 Arc / Stage / ChapterSpec 相关
- active Plot Thread
- active Foreshadowing

默认排除：
- TRANSIENT_DETAIL
- importance <= 2

除非 ChapterSpec 明确需要。

暂不使用 RAG。

---

## 22. Dedup v1

不使用 Embedding。

Current State：
`story + category + subject + field`

Relationship：
`story + A + B`

Story Memory：
优先 exact normalized key/description 去重。

不解决模糊语义重复。

---

## 23. 实施顺序冻结

### Phase 0 — 修正 Evidence Base

- 将 v0.1 状态改为 Engineering Pipeline Accepted
- 记录 Real-LLM behavior 未验收
- 修正 `.agent/STATE.md` 的错误 DONE 声明
- 保存当前真实 Prompt / Payload / Regression Story

Gate：
不得再宣称 v0.1 已证明 Memory 改善正文。

### Phase 1 — Context Wiring + Continuation

- Planner Context v2
- Writer Context v2
- expectedProgress 真正传给 Writer
- Continuation Anchor
- 2–3 Summary + Last Ending
- Request Debug Capture

Gate：
真实 LLM 新 Stage 不重新开书。

### Phase 2 — Chapter Control

- Story 默认字数
- ChapterSpec
- Planner output v2
- Writer Goal Lock
- Length Control
- UI

Gate：
真实 LLM 3000 字目标，5 章至少 4 章在 2250–3750，并完成 ChapterSpec。

### Phase 3 — Generation Reliability

- memoryExtractionStatus
- Extraction Retry
- Async Continuous
- Pause
- Stop
- Stage COMPLETED
- Next Safe Action

Gate：
Chapter 已保存、Extractor 首次失败后 Retry 必须重试同章 Extraction。

### Phase 4 — Replan Remaining

- minimal Plan Version
- active remaining plan
- Replan API/UI
- Job total 一致性

Gate：
9 章写 3 章后把剩余改 3 章，最终无重复得到 6 章。

### Phase 5 — Chapter Revision

- ChapterRevision
- Manual Edit
- Regenerate
- Revision History
- Approve
- Memory stale

Gate：
作者修改后最新正文生效、旧 Draft 保留。

### Phase 6 — Long-form Pace

- targetChapterCount
- Arc
- longFormPosition
- Pace Guard

Gate：
600 章目标第 5 章不得主动推进终局。

### Phase 7 — Memory v2

- Type Enum
- importance
- scope
- TRANSIENT_DETAIL
- dedup
- Inventory fix
- Memory Selection

Gate：
普通面包后续 3 个无关章节不再持续成为核心。

### Phase 8 — Polish + Style

- writingStyle
- Polish AI
- Java Workflow
- Vue UI
- Revision + Memory Refresh

Gate：
Polish 改善表达但保持剧情事实。

### Phase 9 — Full Real-LLM Acceptance

所有语义测试使用真实 LLM 执行并保存证据。

---

## 24. 冻结 Acceptance Suite

### AC-101 Planner Continuation
已认识艾琳并入住后，新 Stage“第二天去冒险者公会”。

PASS：从当前状态继续。
FAIL：重新穿越/初遇/找住所。

### AC-102 Writer Context Wiring
已有数据时，捕获的 Request 中 `currentState / storyMemories / relationshipState` 非空。

该测试允许 Mock。

### AC-103 Chapter Length
目标 3000，真实 LLM 连续 5 章。

PASS：至少 4 章在 2250–3750，且不是明显重复灌水。

### AC-104 Chapter Goal
ChapterSpec 要求完成公会登记并禁止暴露身份。

PASS：正文真实完成要求。

### AC-105 Low-value Detail Isolation
普通面包标记为 `TRANSIENT_DETAIL / 1 / CHAPTER`。

PASS：后续 3 个无关 ChapterSpec 不围绕面包。

### AC-106 Replan Remaining
9 章写完 3 章后，剩余改为 3 章。

PASS：前 3 章不变，最终 6 章，无重复。

### AC-107 Manual Edit
删除正文“获得铁剑”。

PASS：新 Revision 生效，旧 Revision 存在。

### AC-108 Memory Refresh
承接 AC-107。

PASS：铁剑旧 Memory 不再有效。

### AC-109 Polish Fact Preservation
Polish 降低机械表达。

PASS：核心事件、位置、物品、Ending Intent 不变。

### AC-110 Extraction Retry
Writer 成功、Chapter 已保存、Extractor 失败。

PASS：Retry 先补同章 Extraction，再进入下一章。

### AC-111 Async Progress
Continuous Start 快速返回 Job，前端可轮询 1/N → 2/N → ...

### AC-112 Pause
Pause 后在安全 checkpoint 停止，不继续下一章。

### AC-113 Stage Completion
全部 active plan 完成后：
`Job=COMPLETED` 且 `Stage=COMPLETED`。

### AC-114 600 Chapter Pace
真实 LLM，target=600、current=5、Arc=1–60 生存融入。

PASS：不主动安排终局真相/终局大战/最终返回通道。

### AC-115 Memory Dedup
同一长期事实连续重复出现。

PASS：不产生大量等价 StoryMemory。

### AC-116 Inventory Multi-item
先铁剑，再药水。

PASS：两者同时存在，不互相覆盖。

### AC-117 Mock Boundary
Mock 只能证明：
HTTP、DTO、Persistence、Workflow、Retry、Context Wiring。

Mock 不得用于宣称：
文风、规划合理性、Memory 改善正文、长篇节奏、ChapterSpec 语义遵循。

---

## 25. 必须记录的 Real-LLM Metrics

至少：

- Model
- Prompt Version
- Story ID
- Run ID
- Target Characters
- Actual Characters
- Length Pass Rate
- Chapter Goal Completion Rate
- Continuation Failure Count
- Low-value Detail Repetition Count
- Duplicate Memory Count
- Manual Edit Count
- Regeneration Count
- Polish Count
- Memory Extraction Failure Count
- Average Generation Latency

不建设复杂 Eval 平台。

---

## 26. Definition of Done

只有同时满足以下条件才能写：

> **v0.1.1 ACCEPTED**

Engineering：
- Backend tests pass
- Python tests pass
- Frontend build pass
- v0.1 → v0.1.1 migration 可运行
- Extraction Retry 通过
- Async / Pause / Replan 状态一致

Real LLM：
- Continuation PASS
- Length PASS
- Chapter Goal PASS
- Replan Remaining PASS
- Manual Edit PASS
- Polish Fact Preservation PASS
- 600 Chapter Pace PASS
- Low-value Detail Isolation PASS

Evidence：
- 保存 Model / Prompt / Plan / Chapters / Memory / Run Result / Failure Record

---

## 27. 不阻塞 v0.1.1 的问题

以下仍可留到后续：

- 文风达到专业作者水平
- 600 章完全无漂移
- 全模型一致
- RAG 历史全文检索
- 完整 Temporal Truth
- 商业编辑器
- 自动发布
- 100% 无重复
- 每章精确 3000 字

目标不是完美。

目标是：

> 从“AI 自动生成但作者难以控制”，提升到“作者可规划、可编辑、可恢复、可持续验证”。

---

## 28. Coding Agent 新 DONE 规则

从 v0.1.1 起：

> **Code exists ≠ Feature works**

> **DTO exists ≠ Context is wired**

> **Mock passes ≠ AI behavior passes**

> **STATE says DONE ≠ Repository proves DONE**

每个 AI 行为任务必须记录：

```text
Engineering Verification:
PASSED / FAILED

Real-LLM Semantic Verification:
PASSED / FAILED / NOT_REQUIRED / BLOCKED
```

---

## 29. Freeze Change Control

如果 Agent 认为冻结需求无法满足某个现有 Acceptance Test，必须提交：

- Observed blocker
- Why current frozen design is insufficient
- Smallest required change
- Alternatives attempted
- New complexity introduced

只有项目所有者明确批准后才能修改本文件。

---

## 30. Final Frozen Principles

> **AI proposes. Java decides. MySQL remembers.**

> **The plan guides. Memory supports.**

> **Generated text is a draft until the author accepts it.**

> **Mock proves plumbing. Real LLM proves AI behavior.**

以及 v0.1 最重要的工程教训：

> **不要用“所有 Task 都 DONE”代替“产品真的工作”。**
