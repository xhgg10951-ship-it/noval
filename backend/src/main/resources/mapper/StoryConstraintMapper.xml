<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.storyai.story.mapper.StoryConstraintMapper">

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO story_constraint (story_id, type, content, sort_order)
        VALUES
        <foreach collection="list" item="c" separator=",">
            (#{c.storyId}, #{c.type}, #{c.content}, COALESCE(#{c.sortOrder}, 0))
        </foreach>
    </insert>

    <select id="findByStoryId" parameterType="long"
            resultType="com.example.storyai.story.model.StoryConstraint">
        SELECT id, story_id, type, content, sort_order, created_at
        FROM story_constraint
        WHERE story_id = #{storyId}
        ORDER BY sort_order ASC, id ASC
    </select>
</mapper>
