<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.storyai.stage.mapper.ChapterPlanMapper">

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO chapter_plan (stage_id, chapter_order, goal, expected_progress)
        VALUES
        <foreach collection="list" item="p" separator=",">
            (#{p.stageId}, #{p.chapterOrder}, #{p.goal}, #{p.expectedProgress})
        </foreach>
    </insert>

    <select id="findByStageId" parameterType="long"
            resultType="com.example.storyai.stage.model.ChapterPlan">
        SELECT id, stage_id, chapter_order, goal, expected_progress, created_at, updated_at
        FROM chapter_plan
        WHERE stage_id = #{stageId}
        ORDER BY chapter_order ASC
    </select>

    <select id="findById" parameterType="long"
            resultType="com.example.storyai.stage.model.ChapterPlan">
        SELECT id, stage_id, chapter_order, goal, expected_progress, created_at, updated_at
        FROM chapter_plan
        WHERE id = #{id}
    </select>

    <update id="updateGoal">
        UPDATE chapter_plan
        SET goal = #{goal}
        WHERE id = #{id}
    </update>

    <delete id="deleteByStageId" parameterType="long">
        DELETE FROM chapter_plan
        WHERE stage_id = #{stageId}
    </delete>
</mapper>
